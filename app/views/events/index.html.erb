<%= render 'components/header', path: root_path %>
<% if params[:month] %>
<%= render "month", events: @events %>
<% else %>
<%= render "week", events: @events %>
<% end %>
<%= link_to "New event", new_event_path%>